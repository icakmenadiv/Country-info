# ChatGPT 실행 규칙

이 저장소는 OCIS 국별환경의 **갱신 후보 추천용** 대시보드이다.

## 기본 원칙
- OCIS 기존 본문은 저장하지 않는다.
- 기존값/제안값 비교를 하지 않는다.
- UPDATE / KEEP / REVIEW 판정을 하지 않는다.
- 엑셀의 과거 항목명보다 **현재 OCIS 공통 항목체계**를 우선한다.
- 추천 순위는 **현재 OCIS에서 독립 운영 중인 active 항목**의 `ocis_updated_at`만 사용한다.
- `merged`, `retired`, `unknown` 항목은 자동 추천에서 제외한다.

## 공통 구조 적용
2026-09-07 우즈베키스탄(`ntnCd=299`) OCIS 전체 메뉴를 직접 확인하여 `data/structure.json > common`에 현행 공통 항목체계를 저장하였다.
국가별 국별환경은 동일 항목체계를 사용하므로 이를 유럽 8개국과 중앙아시아 5개국에 공통 적용한다.

공통 구조 기준:
- `active` 69개: 현재 독립 상세페이지 운영
- `merged` 11개: 상위 항목 본문에 통합
- `retired` 3개: 현재 독립 메뉴에서 확인되지 않음

대표 merged 항목:
- 정치의 약사/국가제도/정부요인/최근 정치동향/외교노선 → `일반현황 > 정치`
- 주요경제지표/최근 경제동향/경제개발계획 → `일반현황 > 경제`
- 정치·외교/무역/주재기관 → `일반현황 > 우리나라와의 관계`

인프라·플랜트·건설업제도·투자·조세·금융·보험·노동·출입국·공사수행환경·진출현황의 세부항목은 상당수가 현재도 독립 상세페이지이므로 임의로 상위항목에 통합하지 않는다.

## 구조 상태 판정
`data/structure.json`을 기준으로 다음 상태를 사용한다.
- `active`: 현재 OCIS에서 독립 항목으로 운영 중
- `merged`: 과거 세부항목이 상위 active 항목 본문으로 통합됨
- `retired`: 현재 독립 항목에서 제외됨
- `unknown`: 공통 구조에도 없고 별도 판정도 없는 경우

판정 시 정확한 `merged_map`을 우선 사용한다. 단순히 active 상위경로를 가진다는 이유만으로 모든 하위항목을 merged 처리하지 않는다.

## `국별환경 갱신 후보 5개 추천해줘`
1. `data/state.json`에서 enabled=true 대상국을 확인한다.
2. `data/catalog.json`에서 과거 항목별 `ocis_updated_at`을 읽는다.
3. `data/structure.json > common`의 현행 공통 OCIS 구조를 적용한다.
4. active 항목만 자동 추천 후보로 남긴다.
5. merged/retired 항목은 오래된 날짜가 있어도 추천에서 제외한다.
6. 각 국가의 active 항목 중 가장 오래된 `ocis_updated_at`을 구한다.
7. 그 날짜가 가장 오래된 국가 1개를 추천 국가로 고른다.
8. 해당 국가의 active 항목을 `ocis_updated_at` 오름차순으로 정렬해 최대 5개를 제시한다.
9. 저장소 데이터만으로 추천을 요청받은 경우 웹조사를 하지 않는다.

## 국가 지정 명령
`[국가] 갱신 후보 5개 보여줘`
- 해당 국가에 공통 현행 OCIS 구조를 적용한다.
- active 항목만 `ocis_updated_at` 오래된 순으로 최대 5개 제시한다.

## 최신정보 조사 명령
사용자가 추천된 항목에 대해 실제 최신정보 조사까지 요청하면 그때 별도로 웹 또는 브라우저를 조사한다.
- 가능하면 먼저 OCIS 현재 화면을 직접 열어 해당 항목의 실제 내용과 작성일을 확인한다.
- 공식 정부/법령/통계/발주기관 우선
- 필요 시 IFI 및 신뢰도 높은 전문기관 활용
- 출처명, 게시/기준일, 원문 링크 필수

## 구조 정보 갱신 명령
사용자가 OCIS 구조 변화를 발견하면:
1. 한 국가의 현재 OCIS 메뉴에서 항목체계 전체를 확인한다.
2. 국가별 공통 변경으로 판단되면 `data/structure.json > common`을 갱신한다.
3. 특정 국가만의 예외가 실제 확인될 때만 `countries[국가코드]` override를 둔다.
4. `verified_at`을 갱신한다.

## 출력 기본형

### 다음 추천 국가: [국가]
- 국가 내 가장 오래된 active 항목 수정일: YYYY-MM-DD

| 순위 | 항목 | 운영상태 | OCIS 수정일 |
|---|---|---|---|
| 1 | ... | active | ... |

필요 시 아래에 별도로 표시한다.
- 통합된 과거 항목: n개
- 폐지/미확인 항목: n개

토큰 절약을 위해 추천에 필요하지 않은 본문과 과거 이력은 읽지 않는다.
