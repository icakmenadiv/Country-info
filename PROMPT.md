# ChatGPT 실행 규칙

이 저장소는 OCIS 국별환경의 **갱신 후보 추천용** 대시보드이다.

## 기본 원칙
- OCIS 기존 본문은 저장하지 않는다.
- 기존값/제안값 비교를 하지 않는다.
- UPDATE / KEEP / REVIEW 판정을 하지 않는다.
- 엑셀에 존재하는 과거 항목이 현재 OCIS에서 독립 운영되는지 먼저 확인한다.
- 추천 순위는 **현재 OCIS에서 독립 운영 중인 active 항목**의 `ocis_updated_at`만 사용한다.
- `merged`, `retired`, `unknown` 항목은 자동 추천에서 제외한다.

## 구조 상태 판정
`data/structure.json`을 기준으로 다음 상태를 사용한다.
- `active`: 현재 OCIS에서 독립 항목으로 운영 중
- `merged`: 과거 세부항목이 상위 active 항목 본문으로 통합됨
- `retired`: 폐지됨
- `unknown`: 현재 운영 구조 미확인

현재 구조가 검증된 국가에서는 `active_titles`와 정확히 일치하는 항목만 active로 본다.
해당 active 항목의 하위 경로로 존재하는 과거 엑셀 항목은 merged로 본다.
예: `일반현황 > 경제`가 active이면 `일반현황 > 경제 > 최근 경제동향`은 merged로 처리한다.

## `국별환경 갱신 후보 5개 추천해줘`
1. `data/state.json`에서 enabled=true 대상국을 확인한다.
2. `data/catalog.json`에서 과거 항목별 `ocis_updated_at`을 읽는다.
3. `data/structure.json`에서 현재 OCIS 독립 운영 구조를 확인한다.
4. 구조가 검증된 국가의 active 항목만 자동 추천 후보로 남긴다.
5. merged/retired 항목은 오래된 날짜가 있어도 추천에서 제외한다.
6. unknown 국가는 구조 확인이 끝날 때까지 자동 추천하지 않는다.
7. 각 국가의 active 항목 중 가장 오래된 `ocis_updated_at`을 구한다.
8. 그 날짜가 가장 오래된 국가 1개를 추천 국가로 고른다.
9. 해당 국가의 active 항목을 `ocis_updated_at` 오름차순으로 정렬해 최대 5개를 제시한다.
10. 저장소 데이터만으로 추천을 요청받은 경우 웹조사를 하지 않는다.

## 국가 지정 명령
`[국가] 갱신 후보 5개 보여줘`
- 해당 국가의 구조가 검증되어 있으면 active 항목만 `ocis_updated_at` 오래된 순으로 최대 5개 제시한다.
- 구조가 미확인(unknown)이면 임의로 추천하지 말고 `현재 OCIS 운영 구조 확인 필요`라고 표시한다.

## 최신정보 조사 명령
사용자가 추천된 항목에 대해 실제 최신정보 조사까지 요청하면 그때 별도로 웹 또는 브라우저를 조사한다.
- 가능하면 먼저 OCIS 현재 화면을 직접 열어 해당 항목이 실제 독립 운영 중인지 다시 확인한다.
- 공식 정부/법령/통계/발주기관 우선
- 필요 시 IFI 및 신뢰도 높은 전문기관 활용
- 출처명, 게시/기준일, 원문 링크 필수

## 구조 정보 갱신 명령
사용자가 OCIS 구조 변화나 통합 사실을 발견하면:
1. 현재 OCIS 페이지를 직접 확인한다.
2. `data/structure.json`의 해당 국가 `active_titles`를 갱신한다.
3. 과거 하위항목은 가능한 한 별도 수작업 없이 경로 규칙으로 merged 처리한다.
4. 명확히 폐지된 항목만 `retired_titles`에 기록한다.
5. 확인일(`verified_at`)을 갱신한다.

## 출력 기본형

### 다음 추천 국가: [국가]
- 국가 내 가장 오래된 active 항목 수정일: YYYY-MM-DD

| 순위 | 항목 | 운영상태 | OCIS 수정일 |
|---|---|---|---|
| 1 | ... | active | ... |

필요 시 아래에 별도로 표시한다.
- 통합된 과거 항목: n개
- 구조 확인 필요 항목: n개

토큰 절약을 위해 추천에 필요하지 않은 본문과 과거 이력은 읽지 않는다.
