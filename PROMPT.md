# ChatGPT 실행 규칙

이 저장소는 OCIS 국별환경의 **갱신 후보 추천용** 대시보드이다.

## 기본 원칙
- OCIS 기존 본문은 저장하지 않는다.
- 기존값/제안값 비교를 하지 않는다.
- UPDATE / KEEP / REVIEW 판정을 하지 않는다.
- 추천 순위는 `data/updates.json`의 `ocis_updated_at`만 사용한다.

## `국별환경 갱신 후보 5개 추천해줘`
1. `data/state.json`에서 enabled=true 대상국을 확인한다.
2. `data/updates.json`의 항목을 국가별로 묶는다.
3. 각 국가에서 가장 오래된 `ocis_updated_at`을 구한다.
4. 그 날짜가 가장 오래된 국가 1개를 추천 국가로 고른다.
5. 해당 국가의 항목을 `ocis_updated_at` 오름차순으로 정렬해 최대 5개를 제시한다.
6. 날짜가 없는 항목은 자동 순위에서 제외하고 별도 `날짜 미확인`으로만 표시한다.
7. 저장소 데이터만으로 추천을 요청받은 경우 웹조사를 하지 않는다.

## 국가 지정 명령
`[국가] 갱신 후보 5개 보여줘`
- 해당 국가 항목만 `ocis_updated_at` 오래된 순으로 최대 5개 제시한다.

## 최신정보 조사 명령
사용자가 추천된 항목에 대해 실제 최신정보 조사까지 요청하면 그때 별도로 웹을 조사한다.
- 공식 정부/법령/통계/발주기관 우선
- 필요 시 IFI 및 신뢰도 높은 전문기관 활용
- 출처명, 게시/기준일, 원문 링크 필수
- 기존 OCIS 본문을 보유하지 않으므로 `기존 문구 대비 수정안`이라고 표현하지 않는다.
- 대신 `최신화 참고자료` 또는 `갱신 제안 정보`로 제공한다.

## 출력 기본형

### 다음 추천 국가: [국가]
- 국가 내 가장 오래된 OCIS 수정일: YYYY-MM-DD

| 순위 | 항목 | OCIS 수정일 | 원문 |
|---|---|---|---|
| 1 | ... | ... | ... |

토큰 절약을 위해 추천에 필요하지 않은 본문, 과거 이력, 설명은 읽지 않는다.
