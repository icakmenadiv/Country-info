# ChatGPT 실행 규칙

이 저장소는 OCIS 국별환경 최신정보 갱신 PoC이다.

## `국별환경 업데이트 실행`
1. `data/state.json`에서 enabled=true 국가 중 `last_country_check`가 가장 오래된 국가 1개를 선택한다. null은 가장 오래된 것으로 본다.
2. `data/items.json`에서 해당 국가의 항목 중 미해결 PENDING을 제외하고 `last_verified`가 가장 오래된 순으로 최대 5개를 고른다.
3. 각 항목은 기존 `current_value`의 문체, 단위, 길이, 날짜 표기를 참고한다.
4. 최신 웹 자료를 조사하되 `config/policy.json`의 출처 우선순위를 적용한다.
5. 각 항목을 UPDATE / KEEP / REVIEW 중 하나로 판정한다.
6. UPDATE는 기존값과 제안값을 모두 제시한다. KEEP은 최신 근거로 기존 내용이 유효함을 확인한다. REVIEW는 근거 부족/충돌 사유를 적는다.
7. 모든 판정에는 출처명, 기준/게시일, 원문 URL을 포함한다. 원칙적으로 항목당 출처 최대 2개를 사용한다.
8. 결과는 간결하게 작성한다. 조사과정이나 장문의 배경설명은 저장하지 않는다.
9. current_value를 자동으로 덮어쓰지 않는다. UPDATE는 `data/proposals.json`에 PENDING 제안으로 저장한다.
10. KEEP은 검증일 갱신 대상으로, REVIEW는 사람 확인 대상으로 기록한다.

## 수동 명령
- `[국가] 업데이트 실행`: 해당 국가에서 동일 규칙으로 최대 5개 조사
- `[국가] 업데이트 후보 N개 보여줘`: 웹조사 없이 오래된 항목만 조회
- `미반영 업데이트 보여줘`: proposals 중 PENDING만 조회
- `[국가] [항목] 다시 검증해줘`: 해당 항목만 최신 자료로 재검증

## 출력 기본형

### [국가] | [항목]
- 판정: UPDATE / KEEP / REVIEW
- 기존: ...
- 제안: ... (UPDATE인 경우)
- 확인결과: ... (KEEP/REVIEW인 경우)
- 출처: 기관명, 자료명/기준일
- 원문: URL

토큰 절약을 위해 대상 외 항목, 불필요한 국가 설명, 검색 과정은 출력하지 않는다.
